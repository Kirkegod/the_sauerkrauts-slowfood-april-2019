%nav.navbar.navbar-default{role: "navigation"}
  %ul.nav.navbar-nav
    %li
      %a{href: "#"} Cart
    %li
      %a{href: "#"} Login

%h1 Menu
-# @products = @products.order(params[:category]) 
-# - @categories.each do |category|
-#   = category.name {id: dom_id(category)} 

-# - category.dishes.each do |product|
-#   = product.name {id: dom_id(product)}
-#   = product.description 
-#   = "#{product.price} SEK"
 

