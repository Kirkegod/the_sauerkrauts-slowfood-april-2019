%nav.navbar.navbar-default{role: "navigation"}
  %ul.nav.navbar-nav
    %li
      %a{href: "#"} Cart
    %li
      %a{href: "#"} Login

%h1 Menu

= @categories.nil?
= @categories = Category.create([{ name: 'starter'}, { name: 'main'}, { name: 'dessert'}])



- @categories.each do |category|
  = category.name

-# - @categories.each do |category|
-#   = category.name(id:dom_id(category))
  -# - @category.products.each do |product|
  -#   = product.name(id:dom_id(product))
  -#   = product.description 
  -#   = "#{product.price} SEK"
 

